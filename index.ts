/**
 * An example function used to demonstrate unit testing.
 * @param input The value to multiply by 3.
 * @returns The @see {input}, multiplied by 3.
 */
export function exampleFunction(input: number): number {
  return input * 3;
}
